# CONTRIBUTING.md

欢迎参与本项目！请仔细阅读以下贡献指南，以确保协作流程顺畅、高效。

## 目录

- [分支策略](#分支策略)
- [开发流程](#开发流程)
- [提交规范](#提交规范)
- [代码审查](#代码审查)
- [冲突解决](#冲突解决)
- [版本发布](#版本发布)
- [权限与角色](#权限与角色)
- [配置文件示例](#配置文件示例)

---

## 分支策略

我们采用 **功能分支工作流**，结合 `main`、`develop`、`feature/*`、`release/*` 和 `hotfix/*` 分支结构。

### 长期分支

- **`main`**：保护性分支，仅存放稳定、可发布的版本。
- **`develop`**：集成分支，所有新功能最终合并至此。

### 短期分支

- **`feature/功能描述`**：新功能开发分支。
- **`release/vX.X.X`**：版本发布前创建。
- **`hotfix/问题描述`**：紧急修复分支，从 `main` 创建。

## 开发流程

### 1. 初始化

```bash
git clone https://gitee.com/your-team/ChatPDF-Extended.git
cd ChatPDF-Extended
git remote add personal https://gitee.com/your-name/ChatPDF-Extended.git
```

### 2. 开始新功能

```bash
git checkout develop
git pull origin develop
git checkout -b feature/your-feature
```

### 3. 提交更改

提交信息格式如下：

```
<类型>: <简短描述>

<详细描述（可选）>

<关联信息>
```

**类型说明**：

- `feat`：新功能
- `fix`：修复
- `docs`：文档
- `style`：格式
- `refactor`：重构
- `test`：测试
- `chore`：工具或构建变动

示例：

```bash
git commit -m "feat: 实现论文PDF上传接口

- 添加PaperUploadService类
- 实现文件验证和保存逻辑
- 集成原项目PDF解析引擎

关联issue #12"
```

### 4. 推送与同步

```bash
git push personal feature/your-feature
git fetch origin
git rebase origin/develop
```

## 合并请求流程

1. 确保分支是最新的：
   ```bash
   git checkout feature/your-feature
   git rebase origin/develop
   ```

2. 推送到项目仓库：
   ```bash
   git push origin feature/your-feature
   ```

3. 在 Gitee/GitHub 创建 Pull Request：
   - 目标分支：`develop`
   - 描述变更内容
   - 关联相关 Issue
   - 指定审查者

## 代码审查

审查者需检查以下内容：

- [ ] 代码符合编码规范
- [ ] 包含适当的单元测试
- [ ] 文档已更新
- [ ] 不影响现有功能
- [ ] 提交信息清晰规范

## 冲突解决

### 预防冲突

- 频繁同步：
  ```bash
  git fetch origin
  git rebase origin/develop
  ```
- 小步提交，及时沟通

### 解决冲突

```bash
git pull --rebase origin develop
# 手动解决冲突后
git add resolved-file.py
git rebase --continue
```

## 版本发布流程

1. 创建发布分支：
   ```bash
   git checkout -b release/v1.0.0 develop
   ```

2. 更新版本号（如 `setup.py`）

3. 测试并修复问题

4. 合并到 `main` 和 `develop`：
   ```bash
   git checkout main
   git merge --no-ff release/v1.0.0
   git tag -a v1.0.0

   git checkout develop
   git merge --no-ff release/v1.0.0
   ```

5. 删除发布分支：
   ```bash
   git branch -d release/v1.0.0
   ```

## 权限与角色

- **仓库管理员**：保护分支、设置 CI/CD、管理权限
- **架构师**：代码质量监控、审查关键 PR、版本发布管理

## 配置文件示例

### .gitignore

```
.env
.venv/
*.log
logs/
*.db
.cache/
__pycache__/
.vscode/
.idea/
*.pdf
weights/
```

### 提交模板

在 `~/.gitmessage` 中设置：

```
# <类型>: <主题>

# <正文>

# <关联信息>
# 关联issue #
```

启用模板：

```bash
git config --global commit.template ~/.gitmessage
```

---

## 核心原则

- ✅ **主分支保护**：`main` 和 `develop` 受保护，仅通过 PR 合并
- ✅ **功能隔离**：每个功能在独立分支开发
- ✅ **频繁同步**：每日至少同步一次最新代码
- ✅ **规范提交**：提交信息清晰可追溯
- ✅ **代码审查**：所有合并请求必须经过审查
- ✅ **及时备份**：个人分支推送到个人远程仓库

---
感谢你的贡献！🎉

